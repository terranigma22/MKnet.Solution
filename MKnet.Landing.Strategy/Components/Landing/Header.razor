@using MKnet.Landing.Strategy.Domain.Models
@using MKnet.Landing.Strategy.Domain.Services
@inject IJSRuntime JS

<header id="header" class="header d-flex align-items-center fixed-top">
    <div
        class="header-container container-fluid container-xl position-relative d-flex align-items-center justify-content-between">

        <a href="" class="logo d-flex align-items-center me-auto me-xl-0">
            <img src="assets/img/favicon.png" alt="mknet Logo" loading="lazy">

            <div>
                <div class="logo-titles">
                    <h1 class="sitename">MK</h1>
                    <h1 class="sitesurname">nET</h1>
                </div>
                <div class="logo-sub-titles">
                    <span class="logo-sub-title-primary">Punto a punto, <span class="logo-sub-title-secondary">&nbsp;hacemos la diferencia.</span></span>
                </div>
            </div>
        </a>

        <nav id="navmenu" class="navmenu">
            <ul>
                @for (int i = 0; i < sections.Count; i++)
                {
                    if (i == 0)
                    {
                        <li><a href="@sections[i].Link" class="active" @onclick="@ToggleMenu">@sections[i].Title</a></li>
                    }
                    else
                    {
                        <li><a href="@sections[i].Link" @onclick="@ToggleMenu">@sections[i].Title</a></li>
                    }
                }
            </ul>
            <i class="mobile-nav-toggle d-xl-none bi bi-list" @onclick="@ToggleMenu"></i>
        </nav>

        <a class="btn-getstarted" href="#faq">Preguntas</a>

    </div>
</header>

@code {
    private List<SectionDTO> sections = [];

    protected override async Task OnInitializedAsync()
    {
        try
        {
            sections = await JsonHelper.LoadList<SectionDTO>("sections");
        }
        catch (Exception)
        {
            sections = [];
        }
    }

    private async Task ToggleMenu()
    {
        await JS.InvokeVoidAsync("mobileMenuToggle.toggleMenu");
    }
}
